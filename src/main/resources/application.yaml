server:
  port: 8001

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SomeTechStudio/config-repo
          username: SomeTechStudio
          password:

